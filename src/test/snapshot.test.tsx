import { render } from '@testing-library/react';
import App from '../App';

describe ('App, component hierarcy', () => {
    it('renders correctly', () => {
        const tree = render
        
    }
});

